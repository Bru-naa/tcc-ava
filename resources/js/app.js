import './bootstrap';               // arquivo opcional resources/js/bootstrap.js (axios, echo, etc.)
import Alpine from 'alpinejs';      // Alpine
import 'preline';

window.Alpine = Alpine;
Alpine.start();

// Se usar Bootstrap 5 (opcional)
import * as bootstrap from 'bootstrap';